.intro {
    padding: calc( 1em + 2vh) calc( 0.5rem + 2vw);
    text-align: center;
    font-size: 1.1rem;
    border-bottom: 1px solid #EAEAEA;
    &__heading {
        margin: 0.5em 0;
        text-align: center;
        font-family: $font-bold;
    }
    &__desc {
        font-family: $font-bold;
    }
}